#!/usr/bin/env bash
#
# zclaude — run Claude Code against z.ai's Anthropic-compatible GLM API.
#
# Key resolution order:
#   1. `zclaude config [KEY]`     — set/replace the stored key (inline or prompt)
#   2. stored config file         — set on a previous run
#   3. ZAI_API_KEY env var        — used and saved for next time
#   4. interactive prompt         — asks for the key if you haven't included it yet
#
set -euo pipefail

CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/zclaude"
CONFIG_FILE="$CONFIG_DIR/config"

say() { printf '%s\n' "$*" >&2; }

# Trim leading/trailing whitespace.
trim() {
  local s="$1"
  s="${s#"${s%%[![:space:]]*}"}"
  s="${s%"${s##*[![:space:]]}"}"
  printf '%s' "$s"
}

# Persist the key with owner-only permissions.
save_key() {
  local key
  key="$(trim "$1")"
  if [ -z "$key" ]; then
    say "Refusing to save an empty key."
    return 1
  fi
  mkdir -p "$CONFIG_DIR"
  ( umask 077; printf 'ZAI_API_KEY=%s\n' "$key" > "$CONFIG_FILE" )
  chmod 600 "$CONFIG_FILE"
  say "Key saved to $CONFIG_FILE"
}

read_key() {
  [ -f "$CONFIG_FILE" ] || return 0
  local line
  while IFS= read -r line || [ -n "$line" ]; do
    case "$line" in
      ZAI_API_KEY=*) printf '%s' "${line#ZAI_API_KEY=}"; return 0 ;;
    esac
  done < "$CONFIG_FILE"
  return 0
}

# Read a secret from the terminal; fall back to stdin if /dev/tty is unusable.
read_secret() {
  local prompt="$1" value=""
  printf '%s' "$prompt" >&2
  if IFS= read -rs value 2>/dev/null < /dev/tty; then
    :
  else
    IFS= read -rs value || true
  fi
  printf '\n' >&2
  printf '%s' "$value"
}

# Interactive first-time setup.
interactive_setup() {
  say ""
  say "┌──────────────────────────────────────────┐"
  say "│  zclaude · first-time setup               │"
  say "└──────────────────────────────────────────┘"
  say ""
  say "Claude Code will run against z.ai's GLM API."
  say "You only need to enter your key once."
  say "Get a key: https://z.ai (GLM Coding Plan)"
  say ""
  local key="" tries=0
  while :; do
    key="$(trim "$(read_secret 'z.ai API key: ')")"
    [ -n "$key" ] && break
    tries=$((tries + 1))
    say "Key can't be empty."
    if [ "$tries" -ge 3 ]; then
      say "Aborting after 3 empty attempts."
      exit 1
    fi
  done
  save_key "$key"
  say ""
}

# --- subcommands -----------------------------------------------------------
case "${1:-}" in
  config|--config|set-key|--set-key|change|--change|change-key|--change-key)
    if [ "${2:-}" != "" ]; then
      save_key "$2"
    else
      interactive_setup
    fi
    say "Done. Run 'zclaude' to start."
    exit 0
    ;;
  reset|--reset)
    rm -f "$CONFIG_FILE"
    say "Stored key removed."
    exit 0
    ;;
  update|--update|upgrade|--upgrade)
    say "Updating zclaude to the latest version..."
    curl -fsSL "https://raw.githubusercontent.com/sniperprime/zclaude/main/install.sh" | bash
    exit $?
    ;;
esac

# --- resolve the key -------------------------------------------------------
KEY="$(read_key)"

# Not stored yet: take it from the environment if it's there...
if [ -z "${KEY:-}" ] && [ -n "${ZAI_API_KEY:-}" ]; then
  KEY="$(trim "$ZAI_API_KEY")"
  say "Using ZAI_API_KEY from environment; saving for next time."
  save_key "$KEY" || true
fi

# ...otherwise ask for it interactively.
if [ -z "${KEY:-}" ]; then
  interactive_setup
  KEY="$(read_key)"
fi

if [ -z "${KEY:-}" ]; then
  say "No API key available. Run 'zclaude config' to set one."
  exit 1
fi

# --- launch ----------------------------------------------------------------
if ! command -v claude >/dev/null 2>&1; then
  say "claude CLI not found on PATH."
  say "Install Claude Code first: https://docs.claude.com/en/docs/claude-code"
  exit 127
fi

export ANTHROPIC_BASE_URL="https://api.z.ai/api/anthropic"
export ANTHROPIC_AUTH_TOKEN="$KEY"
export ANTHROPIC_MODEL="glm-5.2"
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="glm-5.1"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm-4.7"
export CLAUDE_CODE_SUBAGENT_MODEL="glm-4.7"
export CLAUDE_CODE_EFFORT_LEVEL="max"
export API_TIMEOUT_MS="3000000"

exec claude --dangerously-skip-permissions "$@"
